this.subscriptions.push(
  this.baseCurves$.subscribe(baseCurves => {
    this.BaseCurves = orderByAscending(
      baseCurves.filter(curve => curve.Active), // Filtra los activos
      curve => curve.CurveName                   // Ordena por nombre
    );
  })
);
