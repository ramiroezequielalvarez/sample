<!-- Contenedor externo alineado a la derecha -->
<div style="width: 100%;">
  <div style="display: flex; flex-direction: column; align-items: flex-end; gap: 8px;">
    
    <!-- Reset Grid (estilo aplicado por action-button-holder a) -->
    <div class="action-button-holder">
      <a (click)="onResetGrid()">RESET GRID</a>
    </div>

    <!-- Cancel + Send buttons -->
    <div class="action-button-holder" style="display: flex; gap: 12px; align-items: center;">
      <a (click)="onCancel()">CANCEL</a>

      <button
        mat-flat-button
        class="market-value-update"
        color="primary"
        type="button"
        (click)="onCreateNewConsultantCurve(operationSelected)"
        [disabled]="isSubmitDisabled()">
        SEND FOR APPROVAL
      </button>
    </div>

  </div>
</div>
