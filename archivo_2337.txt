// Ejemplo para daily
this.subscriptions.push(
  this.curvesFacade.dailyCurves$.subscribe(data => {
    if (data && data.length > 0) {
      this.dailyCurvesRowData = data;
    }
  })
);

this.subscriptions.push(
  this.curvesFacade.consultantCurves$.subscribe(data => {
    if (data && data.length > 0) {
      this.consultantCurveRowData = data;
    }
  })
);
