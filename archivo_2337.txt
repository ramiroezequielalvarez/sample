        Type type = typeof(MiClase);

        // Obtener la propiedad privada mediante su nombre
        PropertyInfo propiedadPrivada = type.GetProperty("MiPropiedadPrivada", BindingFlags.NonPublic | BindingFlags.Instance);
